          // ويدجت معدلة لبناء كومة الكتب بطبقات ظلال صحيحة (تكدس حقيقي من الأسفل للأعلى)
          Widget _buildBooksStack() {
            return SizedBox(
              height: 250, // ارتفاع مناسب يحتوي على كل الطبقات المتداخلة
              child: Stack(
                alignment: Alignment.bottomCenter,
                children: [
                  // 5. الكتاب الأكبر في الأسفل (يُبنى أولاً ليكون في الخلفية)
                  Positioned(
                    bottom: 0,
                    child: _buildSingleBook(
                      width: 230,
                      height: 95,
                      gradientColors: [const Color(0xFF223A70), const Color(0xFF335298)],
                      shadowColor: Colors.black.withOpacity(0.3),
                      borderRadius: const BorderRadius.vertical(top: Radius.circular(12)),
                      child: const Padding(
                        padding: EdgeInsets.only(top: 25.0), // إزاحة النص للأسفل لأن الكتاب الذي فوقه سيغطيه جزئياً
                        child: Column(
                          mainAxisAlignment: MainAxisAlignment.center,
                          children: [
                            Text(
                              'DESIGN',
                              style: TextStyle(
                                color: Color(0xFF14244A),
                                fontSize: 22,
                                fontWeight: FontWeight.w900,
                                letterSpacing: 2,
                              ),
                            ),
                            Text(
                              'BY ACCIDENT:\nFOR A NEW',
                              textAlign: TextAlign.center,
                              style: TextStyle(
                                color: Color(0xFF14244A),
                                fontSize: 11,
                                fontWeight: FontWeight.w700,
                                height: 1.1,
                              ),
                            ),
                          ],
                        ),
                      ),
                    ),
                  ),

                  // 4. الكتاب الرابع (الوردي) - يرتفع قليلاً فوق الأزرق
                  Positioned(
                    bottom: 75,
                    child: _buildSingleBook(
                      width: 210,
                      height: 65,
                      gradientColors: [const Color(0xFFE289B3), const Color(0xFFF3A7CC)],
                      shadowColor: Colors.black.withOpacity(0.2),
                      borderRadius: BorderRadius.circular(6),
                      child: const Center(
                        child: Text(
                          'HEY',
                          style: TextStyle(
                            color: Colors.white,
                            fontSize: 18,
                            fontWeight: FontWeight.bold,
                            letterSpacing: 2,
                          ),
                        ),
                      ),
                    ),
                  ),

                  // 3. الكتاب الثالث (الأزرق الداكن)
                  Positioned(
                    bottom: 125,
                    child: _buildSingleBook(
                      width: 195,
                      height: 38,
                      gradientColors: [const Color(0xFF1C1C3A), const Color(0xFF2A2A54)],
                      shadowColor: Colors.black.withOpacity(0.25),
                      borderRadius: BorderRadius.circular(5),
                      child: const Center(
                        child: Text(
                          'DAVID CROW',
                          style: TextStyle(
                            color: Color(0xFF8A8AAB),
                            fontSize: 10,
                            fontWeight: FontWeight.w500,
                            letterSpacing: 1.5,
                          ),
                        ),
                      ),
                    ),
                  ),

                  // 2. الكتاب الثاني (الرمادي)
                  Positioned(
                    bottom: 153,
                    child: _buildSingleBook(
                      width: 175,
                      height: 34,
                      gradientColors: [const Color(0xFFB0B0B5), const Color(0xFFD1D1D6)],
                      shadowColor: Colors.black.withOpacity(0.15),
                      borderRadius: BorderRadius.circular(4),
                      child: const Center(
                        child: Text(
                          'CRITIC',
                          style: TextStyle(
                            color: Color(0xFF8E8E93),
                            fontSize: 14,
                            fontWeight: FontWeight.w800,
                            letterSpacing: 3,
                          ),
                        ),
                      ),
                    ),
                  ),

                  // 1. الكتاب الأول والأصغر في الأعلى (يركب فوق الجميع ويلقي ظله عليهم)
                  Positioned(
                    bottom: 177,
                    child: _buildSingleBook(
                      width: 145,
                      height: 24,
                      gradientColors: [const Color(0xFF9E1A1A), const Color(0xFFBD2424)],
                      shadowColor: Colors.black.withOpacity(0.2),
                      borderRadius: BorderRadius.circular(4),
                    ),
                  ),
                ],
              ),
            );
          }
